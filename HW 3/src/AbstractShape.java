import java.awt.Color;
import java.awt.Graphics;

public abstract class AbstractShape implements Shape {
	// Add instance variables, additional methods and default 
	// implementations here once it becomes clear that all Shapes need to have such fields and methods.
	int x, y;
	Color c;
}
